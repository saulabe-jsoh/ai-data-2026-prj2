# ai-data-2026-proj2

FastAPI와 Jinja2 예제 프로젝트입니다.

설치 및 실행:

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
uvicorn main:app --reload
```

브라우저에서 http://127.0.0.1:8000/ 를 열면 템플릿이 표시됩니다.
